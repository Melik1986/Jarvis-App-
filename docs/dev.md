# 🧠 AXON Developer Guide

## 📖 Введение

**AXON** — это универсальная AI ERP операционная система, построенная на стеке **Replit Native**. Проект представляет собой монорепозиторий, объединяющий мобильное приложение (Expo/React Native) и бэкенд (Nest.js) в единой кодовой базе.

### Ключевые особенности

- **AI Adapter Pattern**: Динамическое преобразование естественного языка в API-вызовы ERP систем.
- **Offline-First**: Работа с данными (остатки, задачи) без интернета.
- **RAG (Retrieval-Augmented Generation)**: Поиск по базе знаний компании.
- **Voice & Vision**: Голосовое управление и распознавание документов.

---

## 🏗️ Архитектура проекта

Проект использует упрощенную структуру монорепозитория (Single Package Monorepo), где все зависимости находятся в корневом `package.json`.

### Структура директорий

```plaintext
d:\Dev\Axon-App\
├── client/                 # Мобильное приложение (Frontend)
│   ├── components/         # UI компоненты (React Native, миграция на Tamagui запланирована)
│   ├── hooks/              # Кастомные хуки (бизнес-логика)
│   ├── navigation/         # Навигация (React Navigation v7)
│   ├── screens/            # Экраны приложения
│   ├── store/              # State Management (Zustand)
│   ├── lib/                # Утилиты (React Query, API client)
│   └── App.tsx             # Точка входа Mobile
├── server/                 # Бэкенд (Backend)
│   ├── src/
│   │   ├── modules/        # Модульная архитектура Nest.js
│   │   │   ├── auth/       # Авторизация (Supabase/Passport)
│   │   │   ├── chat/       # AI Chat & WebSocket
│   │   │   ├── erp/        # AI Adapter & ERP Connectors
│   │   │   ├── llm/        # Интеграция с LLM (OpenAI/Vercel SDK)
│   │   │   └── rag/        # Vector Store (Qdrant)
│   │   └── main.ts         # Точка входа Server
├── shared/                 # Общий код
│   ├── types.ts            # Изоморфные типы для Client и Server
│   └── schema.ts           # Drizzle schema (Server-only)
├── docs/                   # Документация проекта
├── package.json            # Единый файл зависимостей
└── tsconfig.json           # Конфигурация TypeScript
```

### Технологический стек

| Слой         | Технологии                                                              |
| ------------ | ----------------------------------------------------------------------- |
| **Mobile**   | React Native 0.81, Expo 54, React Navigation 7, Zustand, TanStack Query |
| **Backend**  | Nest.js 11, Express, Passport, Vercel AI SDK                            |
| **Database** | PostgreSQL (Supabase), Drizzle ORM                                      |
| **AI**       | OpenAI (GPT-4o), Whisper (Voice), Qdrant (Vector DB)                    |
| **Tools**    | TypeScript, SWC (Fast Compile), Biome/Prettier                          |

Tamagui установлен в зависимостях, миграция UI на Tamagui запланирована после стабилизации базового интерфейса.

---

## 🚀 Настройка окружения (Setup)

### 1. Предварительные требования

- **Node.js**: v20.x или выше.
- **Package Manager**: `npm` (или `bun`).
- **База данных**: Supabase (PostgreSQL).
- **AI Ключи**: OpenAI API Key.

### 2. Установка зависимостей

Выполните в корне проекта:

```bash
npm install
```

### 3. Конфигурация (.env)

Создайте файл `.env` в корне проекта (или используйте Replit Secrets). Пример конфигурации:

```env
# Database (Supabase Transaction Pooler)
DATABASE_URL=postgres://postgres.xxxx:password@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

# AI Provider
OPENAI_API_KEY=sk-proj-xxxx

# App Config
EXPO_PUBLIC_DOMAIN=https://your-repl-url.replit.app
NODE_ENV=development
SESSION_SECRET=super-secret-key
```

### 4. скрипты

Рекомендуется обновить секцию `scripts` в `package.json`:

```json
"scripts": {
  "server:dev": "NODE_ENV=development npx ts-node --swc -P server/tsconfig.json server/src/main.ts",
  "server:nest:dev": "NODE_ENV=development tsx server/src/main.ts"
}
```

---

## Гибридная разработка (Replit + локальная IDE)

### Святые файлы (НЕ ТРОГАТЬ в IDE)

Не редактируйте локально в Cursor/VS Code:

- **`.replit`** — инструкции запуска и workflow Replit.
- **`replit.nix`** — системные зависимости (Nix); при отсутствии в репозитории может создаваться Replit.
- **`.config/`** — конфигурация окружения Replit.

Изменения вносите только на Replit или осторожно переносите вручную. При расхождении версий зависимостей на Replit после вашего push — на Replit выполните переустановку (см. ниже); **`package-lock.json`** в .gitignore не добавляем (важен для CI), но без явной задачи его лучше не менять — новую библиотеку после push установите на Replit через `npm install`.

### Как не сломать Replit при push

- Не меняйте святые файлы локально и не выполняйте `git rm --cached` для `.replit` или `replit.nix` — иначе при следующем pull на Replit конфиг запуска пропадёт из репозитория.
- Конфликты по lock-файлу: если на Replit после pull ругается на зависимости — удалите на Replit `node_modules` и `package-lock.json`, выполните `npm install` и нажмите Run.
- Файл **`.env`** не коммитить (уже в .gitignore). Секреты на Replit храните в панели Secrets.

### Пошаговый локальный запуск (за 15 минут)

1. **Секреты:** скопируйте переменные в `.env` из Replit Secrets (или создайте по примеру из раздела «Конфигурация (.env)» выше).
2. **Зависимости:** в корне проекта один раз выполните `npm install` (проект — single package, зависимости в корневом `package.json`).
3. **Бэкенд:** в корне выполните `npm run server:dev` (или `npm run server:nest:dev`). Успешный старт — сообщение вроде «Nest application successfully started».
4. **Фронт:** в корне выполните `npx expo start` (или `npm run expo:dev`), затем выберите `i` / `a` или отсканируйте QR-код в Expo Go.

Если при `npm run server:dev` падает с ошибкой — проверьте, что в `.env` заданы все нужные переменные (например `DATABASE_URL`, `OPENAI_API_KEY`, `SESSION_SECRET`, `EXPO_PUBLIC_DOMAIN` и др. по разделу «Конфигурация (.env)»).

---

## ▶️ Запуск проекта

### Режим разработки (Development)

Рекомендуется запускать клиент и сервер в разных терминалах.

**Терминал 1: Бэкенд**

```bash
npm run server:dev
```

_Сервер запустится на порту 3000 (или 5000 в зависимости от конфигурации)._

**Терминал 2: Мобильный клиент**

```bash
npm run expo:dev
```

_Запустится Expo CLI. Отсканируйте QR-код приложением Expo Go на телефоне._

---

## 🛠️ Разработка и расширение

### Добавление новой интеграции ERP (AI Adapter)

Логика адаптеров находится в `server/src/modules/erp`.

1.  **Создайте спецификацию**: Поместите OpenAPI/Swagger JSON спецификацию новой системы (например, Odoo или SAP) в папку конфигураций.
2.  **Обновите `ErpService`**:
    - Внедрите загрузку спецификации.
    - Vercel AI SDK автоматически использует `tools` (function calling) на основе этой спецификации.
3.  **Тестирование**: Запросите у чат-бота действие, связанное с новой системой (например, "Создай накладную в Odoo").

### Работа с базой данных (Drizzle)

Схема БД определена в `shared/schema.ts`.

1.  Измените схему в `shared/schema.ts`.
2.  Примените изменения:
    ```bash
    npm run db:push
    ```
    _Команда синхронизирует схему с базой данных Supabase._

### Добавление нового экрана (Expo)

1.  Создайте компонент экрана в `client/screens/NewScreen.tsx`.
2.  Добавьте маршрут в `client/navigation/RootStackNavigator.tsx` (или другой навигатор).
3.  Используйте `useScreenOptions` для настройки заголовка.

---

## 🐛 Troubleshooting (Решение проблем)

### 1. Ошибка подключения к БД (Connection refused)

**Причина**: Неверный `DATABASE_URL` или блокировка IP.
**Решение**: Проверьте, что используете Transaction Pooler (порт 6543) от Supabase, так как прямое соединение (порт 5432) не работает в Serverless средах (Replit) из-за отсутствия IPv6.

### 2. Ошибка "Expo Go - Network Error"

**Причина**: Телефон и компьютер (Replit) не видят друг друга.
**Решение**: Убедитесь, что `EXPO_PUBLIC_DOMAIN` установлен корректно. Если используете Replit, туннелирование происходит автоматически, но иногда требуется перезапуск `npm run expo:dev`.

### 3. TypeScript ошибки в Shared модуле

**Причина**: Разные конфиги TS для клиента и сервера.
**Решение**: общий слой разделен на `shared/types.ts` (изоморфные типы) и `shared/schema.ts` (серверный Drizzle schema). Клиент использует только `@shared/types`.

### 5. Потенциальные проблемы и риски

1. Hardcoded OData Paths: В текущем erp.service.ts жестко прописаны пути к регистрам конкретной ERP (например, 1С:УНФ). Это будет работать только с типовыми конфигурациями. Для универсальности требуется парсинг $metadata или OpenAPI спеки.
2. Хранение эмбеддингов: В schema.ts поле embedding имеет тип text . Это неэффективно для векторного поиска. Необходимо использовать расширение vector в Postgres.
3. Монорепозиторий без Workspaces: Все зависимости свалены в один package.json . При росте проекта могут возникнуть конфликты версий (например, разные версии React для Web-админки и Mobile).

---

## 📚 API Reference

### Основные эндпоинты

- `POST /api/conversations/:id/messages`: Чат со стримингом ответа AI.
  - Body: `{ content, llmSettings?, erpSettings?, ragSettings? }`
  - Response: SSE stream (Text + Tool Calls).
- `POST /api/conductor/parse`: Парсинг сырого текста (без стриминга) — для теста «Дирижёра» в Swagger.
  - Body: `{ rawText, llmSettings?, erpSettings?, ragSettings? }`
  - Response: `{ rawText, toolCalls: [{ toolName, args, resultSummary }], assistantText }`.
- `POST /api/auth/login`, `GET /api/auth/callback`, `POST /api/auth/refresh`, `POST /api/auth/logout`: Авторизация.
- Swagger UI: **`/api/docs`** — интерактивная документация всех эндпоинтов.

---

## 🧪 Тестирование «Дирижёра» и окружение

### Verbose Logging (сквозной путь)

Чтобы в консоли сервера видеть выбранный baseURL, факт запроса к LLM и JSON по каждому вызову инструмента (tool-call / tool-result):

- В dev логи включены автоматически (`NODE_ENV=development`).
- В проде: задайте переменную окружения `CONDUCTOR_VERBOSE_LOG=1`.

Логи имеют префикс `[Conductor]` и не содержат API-ключей.

### Swagger (тест парсера без мобильного приложения)

1. Запустите бэкенд: `npm run server:dev` (или `npm run server:nest:dev`).
2. Откройте в браузере: **`https://ваш-хост/api/docs`** (на Replit — `https://ваш-repl.replit.app/api/docs`).
3. Для проверки «Дирижёра» вызовите **POST /api/conductor/parse** с телом, например: `{ "rawText": "три колы и один пирожок" }`.
4. В ответе будут `toolCalls` (структурированные вызовы) и `assistantText`.

### Демо-режим ERP (без 1С)

Для демо или судей без доступа к реальной 1С:

1. В приложении: **Profile → ERP Connection** (или настройки ERP).
2. Выберите режим **Demo** (вместо «1C / Connect»).
3. Кнопка подтверждения (Confirm) будет работать с локальным mock: остатки, товары и создание «накладной» без запросов к 1С.

На бэкенде при `provider === 'demo'` или при отсутствии `baseUrl` ErpService использует mock-данные.

### Проверка туннелинга (Expo ↔ Replit)

Перед конкурсом или демо:

1. **Replit**: в настройках Repl включите доступ по сети (например, Public), чтобы телефон мог достучаться до бэкенда.
2. **Клиент**: переменная `EXPO_PUBLIC_DOMAIN` должна указывать на публичный URL Replit (или туннель).
3. Проверка: откройте в браузере (с ПК или телефона) `https://ваш-хост/api/docs` и выполните **POST /api/conductor/parse** или **POST /api/conversations** — убедитесь, что запросы доходят.

### Ручная проверка перед демо / релизом

Приложение рассчитано на запуск в среде **Replit** с **Expo Go**: пакетер и API привязаны к `REPLIT_DEV_DOMAIN` (см. скрипт `expo:dev` в [package.json](../package.json)).

**Чек-лист:**

1. **Запуск на Replit**  
   Через workflow **Start App** (или по отдельности **Start Backend** и **Start Frontend**). Бэкенд: `npm run server:dev`, порт 5000. Фронт: `npm run expo:dev` (в скрипте уже заданы `REACT_NATIVE_PACKAGER_HOSTNAME=$REPLIT_DEV_DOMAIN`, `EXPO_PUBLIC_DOMAIN=$REPLIT_DEV_DOMAIN:5000`).

2. **Swagger**  
   Открыть в браузере `https://<REPLIT_DEV_DOMAIN>/api/docs` (или публичный URL Repl). Вызвать **POST /api/conductor/parse**, body: `{ "rawText": "три колы и один пирожок" }`. Проверить: ответ 200, в теле есть `toolCalls` (массив с хотя бы одним вызовом), у элементов есть `args` и `resultSummary`, есть `assistantText`.

3. **Туннель / Expo Go**  
   Repl в режиме Public. На телефоне в Expo Go подключиться по URL Replit (QR или ссылка из вывода `expo:dev`). Убедиться, что запросы с телефона доходят до API (чат или Conductor из приложения).

4. **Демо-режим ERP в приложении**  
   В приложении: **Profile → ERP Connection** (или экран настроек ERP). Режим **Demo**. Открыть чат, отправить фразу вроде «три колы и один пирожок», дождаться ответа с предложением накладной, нажать **Confirm** — создание без реальной 1С (mock).

### Запуск тестов

- **Все тесты**: `npm test`.
- **Только бэкенд (NestJS + Jest)**: `npm run test:server`.
- **Клиент (валидация Review, Provider Switcher)**: `npx jest --testPathPattern="client/(lib|store)/"`.

Бэкенд-тесты: Orchestrator (LlmService — подстановка baseURL/apiKey, валидация provider–URL), Data Mapper (`parseToolArgs` в `server/src/modules/chat/tool-args.ts`). Клиент: валидация данных перед отправкой в 1С (`validateReviewData`), обновление настроек провайдера без утечки старых ключей (settingsStore).

### Ошибки LLM (плохой провайдер)

При неверном API-ключе или недоступном URL провайдера приложение возвращает единый формат:

- `statusCode: 502`, `code: "LLM_PROVIDER_ERROR"`, `message: "Провайдер недоступен, проверьте настройки ключа"`.
- В мобильном UI показывается это сообщение вместо сырого stack или общего «Failed to process message».

Глобальный фильтр: `server/src/filters/llm-provider-exception.filter.ts`.

---

_Документ создан: 31.01.2026_
_Автор: AXON Core Team_
