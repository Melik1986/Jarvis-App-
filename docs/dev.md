# 🧠 AXON Developer Guide

## 📖 Введение

**AXON** — это универсальная AI ERP операционная система, построенная на стеке **Replit Native**. Проект представляет собой монорепозиторий, объединяющий мобильное приложение (Expo/React Native) и бэкенд (Nest.js) в единой кодовой базе.

### Ключевые особенности

- **AI Adapter Pattern**: Динамическое преобразование естественного языка в API-вызовы ERP систем.
- **Offline-First**: Работа с данными (остатки, задачи) без интернета.
- **RAG (Retrieval-Augmented Generation)**: Поиск по базе знаний компании.
- **Voice & Vision**: Голосовое управление и распознавание документов.

---

## 🏗️ Архитектура проекта

Проект использует упрощенную структуру монорепозитория (Single Package Monorepo), где все зависимости находятся в корневом `package.json`.

### Структура директорий

```plaintext
d:\Dev\Axon-App\
├── client/                 # Мобильное приложение (Frontend)
│   ├── components/         # UI компоненты (React Native, миграция на Tamagui запланирована)
│   ├── hooks/              # Кастомные хуки (бизнес-логика)
│   ├── navigation/         # Навигация (React Navigation v7)
│   ├── screens/            # Экраны приложения
│   ├── store/              # State Management (Zustand)
│   ├── lib/                # Утилиты (React Query, API client)
│   └── App.tsx             # Точка входа Mobile
├── server/                 # Бэкенд (Backend)
│   ├── src/
│   │   ├── modules/        # Модульная архитектура Nest.js
│   │   │   ├── auth/       # Авторизация (Supabase/Passport)
│   │   │   ├── chat/       # AI Chat & WebSocket
│   │   │   ├── erp/        # AI Adapter & ERP Connectors
│   │   │   ├── llm/        # Интеграция с LLM (OpenAI/Vercel SDK)
│   │   │   └── rag/        # Vector Store (Qdrant)
│   │   └── main.ts         # Точка входа Server
├── shared/                 # Общий код
│   ├── types.ts            # Изоморфные типы для Client и Server
│   └── schema.ts           # Drizzle schema (Server-only)
├── docs/                   # Документация проекта
├── package.json            # Единый файл зависимостей
└── tsconfig.json           # Конфигурация TypeScript
```

### Технологический стек

| Слой         | Технологии                                                              |
| ------------ | ----------------------------------------------------------------------- |
| **Mobile**   | React Native 0.81, Expo 54, React Navigation 7, Zustand, TanStack Query |
| **Backend**  | Nest.js 11, Express, Passport, Vercel AI SDK                            |
| **Database** | PostgreSQL (Replit Postgres или любой провайдер), Drizzle ORM           |
| **AI**       | OpenAI (GPT-4o), Whisper (Voice), Qdrant (Vector DB)                    |
| **Tools**    | TypeScript, SWC (Fast Compile), Biome/Prettier                          |

Tamagui установлен в зависимостях, миграция UI на Tamagui запланирована после стабилизации базового интерфейса.

---

## 🚀 Настройка окружения (Setup)

### 1. Предварительные требования

- **Node.js**: v20.x или выше.
- **Package Manager**: `npm` (или `bun`).
- **База данных**: PostgreSQL (на Replit — Replit Postgres; локально — любой провайдер: Replit, Supabase, Neon и т.д.).
- **AI Ключи**: OpenAI API Key.

### 2. Установка зависимостей

Выполните в корне проекта:

```bash
npm install
```

### 3. Конфигурация (.env)

Создайте файл `.env` в корне проекта (или используйте Replit Secrets). Пример конфигурации:

```env
# Database: любая PostgreSQL. На Replit — Replit Postgres (Secrets → DATABASE_URL).
# Локально — любой провайдер (Supabase, Neon и т.д.).
DATABASE_URL=postgres://user:password@host:5432/dbname

# AI Provider
OPENAI_API_KEY=sk-proj-xxxx

# App Config
EXPO_PUBLIC_DOMAIN=https://your-repl-url.replit.app
NODE_ENV=development
SESSION_SECRET=your-secure-session-secret-key-here
```

### 4. скрипты

Рекомендуется обновить секцию `scripts` в `package.json`:

```json
"scripts": {
  "server:dev": "NODE_ENV=development npx ts-node --swc -P server/tsconfig.json server/src/main.ts",
  "server:nest:dev": "NODE_ENV=development tsx server/src/main.ts"
}
```

---

## ▶️ Запуск проекта

### Режим разработки (Development)

Рекомендуется запускать клиент и сервер в разных терминалах.

**Терминал 1: Бэкенд**

```bash
npm run server:dev
```

_Сервер запустится на порту 3000 (или 5000 в зависимости от конфигурации)._

**Терминал 2: Мобильный клиент**

```bash
npm run expo:dev
```

_Запустится Expo CLI. Отсканируйте QR-код приложением Expo Go на телефоне._

---

## 🖥️ Локальный запуск (без Replit)

Запуск в IDE на своей машине **без изменений кода** (только конфиг и скрипты).

### 1. Переменные окружения (.env)

В корне проекта создайте/обновите `.env`:

```env
# Database: любая PostgreSQL. На Replit — Replit Postgres.
DATABASE_URL=postgres://...

# AI (для чата и Conductor)
OPENAI_API_KEY=sk-proj-...

# Локальный бэкенд — обязательно для клиента
EXPO_PUBLIC_DOMAIN=http://localhost:5000

# Для теста на телефоне (Expo Go) укажите IP ПК в той же Wi‑Fi сети, порт сервера 5000
# EXPO_PUBLIC_DOMAIN=http://192.168.1.100:5000

NODE_ENV=development
SESSION_SECRET=local-dev-secret
```

- **Симулятор/эмулятор**: `EXPO_PUBLIC_DOMAIN=http://localhost:5000`.
- **Телефон (Expo Go)**: замените на IP вашего ПК, например `http://192.168.1.100:5000` (порт 5000 — сервер по умолчанию).

### 2. Запуск сервера

**PowerShell (Windows):**

```powershell
$env:NODE_ENV="development"; npm run server:dev
```

**cmd:** переменная не пробросится; можно запускать так (Nest подхватит development из кода/других мест) или установить `cross-env` и в скрипте использовать `cross-env NODE_ENV=development`.

**Bash / WSL:**

```bash
NODE_ENV=development npm run server:dev
```

Сервер слушает порт **5000** (или `PORT` из `.env`). Проверка: <http://localhost:5000/api/docs> (Swagger).

### 3. Запуск клиента (Expo)

В **втором** терминале:

```bash
npm run expo:dev:local
```

Скрипт `expo:dev:local` не использует `REPLIT_DEV_DOMAIN`; `EXPO_PUBLIC_DOMAIN` берётся из `.env`. Откройте приложение в Expo Go по QR или в симуляторе.

### 4. Что не будет работать локально без доп. настроек

- **Replit Auth** (OAuth через Replit): без Replit-домена логин через Replit недоступен. Ожидаемое поведение — использовать Supabase/Phone OTP или тестовый режим без авторизации там, где это предусмотрено.
- **Replit LLM** (провайдер `replit`): в настройках приложения выберите другой провайдер (OpenAI, Groq, Ollama и т.д.) и укажите свои ключи/URL.
- **RAG «Replit PostgreSQL»**: выберите Qdrant/Supabase или отключите RAG, если не настроены.

Код не переписываем: CORS уже разрешает `localhost`/`127.0.0.1`, сервер читает `.env` из корня (`envFilePath: [".env", "../.env"]` в `app.module.ts`).

### 5. Ошибка «EADDRINUSE: address already in use :::5000»

Порт 5000 занят (другой процесс или предыдущий запуск сервера).

- **Вариант А**: Завершить процесс на порту 5000.  
  **Windows (PowerShell от имени администратора):**
  ```powershell
  netstat -ano | findstr :5000
  taskkill /PID <PID> /F
  ```
  Подставь PID из вывода `netstat`.
- **Вариант Б**: Запустить сервер на другом порту. В `.env` добавь `PORT=5001`. Тогда клиент должен ходить на тот же порт: `EXPO_PUBLIC_DOMAIN=http://10.81.144.126:5001` (или `http://localhost:5001` для эмулятора).

---

## 🛠️ Разработка и расширение

### Добавление новой интеграции ERP (AI Adapter)

Логика адаптеров находится в `server/src/modules/erp`.

1. **Создайте спецификацию**: Поместите OpenAPI/Swagger JSON спецификацию новой системы (например, Odoo или SAP) в папку конфигураций.
2. **Обновите `ErpService`**:
   - Внедрите загрузку спецификации.
   - Vercel AI SDK автоматически использует `tools` (function calling) на основе этой спецификации.
3. **Тестирование**: Запросите у чат-бота действие, связанное с новой системой (например, "Создай накладную в Odoo").

### Работа с базой данных (Drizzle)

Схема БД определена в `shared/schema.ts`.

1. Измените схему в `shared/schema.ts`.
2. Примените изменения:

   ```bash
   npm run db:push
   ```

   _Команда синхронизирует схему с базой данных Supabase._

### Добавление нового экрана (Expo)

1. Создайте компонент экрана в `client/screens/NewScreen.tsx`.
2. Добавьте маршрут в `client/navigation/RootStackNavigator.tsx` (или другой навигатор).
3. Используйте `useScreenOptions` для настройки заголовка.

---

## 🐛 Troubleshooting (Решение проблем)

### 1. Ошибка подключения к БД (Connection refused)

**Причина**: Неверный `DATABASE_URL` или блокировка IP.
**Решение**: Проверьте, что используете Transaction Pooler (порт 6543) от Supabase, так как прямое соединение (порт 5432) не работает в Serverless средах (Replit) из-за отсутствия IPv6.

### 2. Ошибка "Expo Go - Network Error"

**Причина**: Телефон и компьютер (Replit) не видят друг друга.
**Решение**: Убедитесь, что `EXPO_PUBLIC_DOMAIN` установлен корректно. Если используете Replit, туннелирование происходит автоматически, но иногда требуется перезапуск `npm run expo:dev`.

### 3. TypeScript ошибки в Shared модуле

**Причина**: Разные конфиги TS для клиента и сервера.
**Решение**: общий слой разделен на `shared/types.ts` (изоморфные типы) и `shared/schema.ts` (серверный Drizzle schema). Клиент использует только `@shared/types`.

### 5. Потенциальные проблемы и риски

1. Hardcoded OData Paths: В текущем erp.service.ts жестко прописаны пути к регистрам конкретной ERP (например, 1С:УНФ). Это будет работать только с типовыми конфигурациями. Для универсальности требуется парсинг $metadata или OpenAPI спеки.
2. Хранение эмбеддингов: В schema.ts поле embedding имеет тип text . Это неэффективно для векторного поиска. Необходимо использовать расширение vector в Postgres.
3. Монорепозиторий без Workspaces: Все зависимости свалены в один package.json . При росте проекта могут возникнуть конфликты версий (например, разные версии React для Web-админки и Mobile).

---

## 📚 API Reference

### Основные эндпоинты

- `POST /api/chat`: Основной эндпоинт для общения с AI.
  - Body: `{ messages: Message[] }`
  - Response: Stream (Text + Tool Calls).
- `POST /api/auth/login`: Вход через Supabase/Phone.
- `GET /api/erp/schema`: Получение текущей схемы ERP для отладки.

---

_Документ создан: 31.01.2026_
_Автор: AXON Core Team_
